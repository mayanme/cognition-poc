export function DemoBanner() {
  return (
    <div className="demo-banner">
      <strong>DEMO STUB LOGIN.</strong> Authentication here is fake: you pick a user from a seeded list, there are no
      passwords and no SSO. All data is fake seeded data — this app never touches real customer data. In production this
      would be replaced by real SSO plus proper session management.
    </div>
  );
}
